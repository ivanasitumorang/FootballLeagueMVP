<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.footballleaguemvp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.favoritelist.FavoriteListActivity"></activity>
        <activity android:name=".ui.teamdetail.TeamDetailActivity" />
        <activity android:name=".ui.matchdetail.MatchDetailActivity" />
        <activity android:name=".ui.searchmatch.SearchActivity" />
        <activity android:name=".ui.matchlist.MatchListActivity" />
        <activity android:name=".ui.leaguedetail.LeagueDetailActivity" />
        <activity android:name=".ui.leaguelist.LeagueListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>